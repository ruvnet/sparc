interface:
  display_name: "SPARC Delivery"
  short_description: "Run a gated SPARC engineering workflow"
  default_prompt: "Use $sparc to deliver this engineering goal through verified phase gates."
dependencies:
  tools:
    - type: "mcp"
      value: "sparc"
      description: "Deterministic SPARC run state, gates, evidence, and trace"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
